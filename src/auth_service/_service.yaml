name: auth-service
protocol: TCP
port: 80
targetPort: 8080

profiles:
  stable:
    max_memory: 1G
    max_cpu: 1
  default:
    max_memory: 0.25G
    max_cpu: 0.25

branches:
  master: stable
  dev/*: default